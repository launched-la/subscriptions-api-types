{
  "name": "@launched-la/subscriptions-api-types",
  "version": "0.0.0",
  "description": "The Typescript types for subscription-api",
  "author": "Ethan Jon",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/launched-la/subscriptions-api-types.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "exports": {
    ".": {
      "types": "./types/index.d.ts"
    }
  },
  "types": "./types/index.d.ts",
  "scripts": {
    "build": "rm -rf types && tsc",
    "watch": "tsc -w true",
    "prepack": "npm run build",
    "fix": "prettier --config .prettierrc 'src/**/*.ts' --write"
  },
  "devDependencies": {
    "@launchedla/snacktime": "^0.0.36",
    "@tsconfig/node16": "^1.0.3",
    "prettier": "^2.7.1",
    "typescript": "^4.9.4"
  },
  "volta": {
    "node": "16.13.1"
  }
}
